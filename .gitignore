# Python
__pycache__/
*.py[cod]
*.so
.Python
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data files (terlalu besar untuk Git)
data/raw/*.csv
data/raw/*.xlsx
data/interim/*
data/processed/*
!data/**/README.md
!data/**/.gitkeep

# Models & artifacts
# Commit: model_final.joblib, preprocessor.joblib, model_config.json
#   (kecil & dibutuhkan API saat repo di-clone)
# Abaikan: data split (besar, regenerable dari notebook Hari 2)
models/split_data.joblib

# MLflow (tracking lokal - reproducible, tak perlu di-commit)
mlflow.db
mlartifacts/
mlruns/

# IDE
.vscode/
.idea/
.DS_Store

# OS
Thumbs.db

# Environment variables
.env

# HF upload staging (temporary, dibuat & dihapus oleh scripts/upload_initial_model.py)
hf_upload/

# SDV synthesizer artifact (regenerable via ml/generate_synthetic.py)
ml/data/synthetic/synthesizer.pkl
